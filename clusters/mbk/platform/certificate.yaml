apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard
  namespace: networking
spec:
  secretName: wildcard
  dnsNames:
    - "*.excloo.com"
    - "*.mbk.excloo.dev"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns01
