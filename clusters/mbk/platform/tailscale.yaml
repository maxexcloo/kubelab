apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
  namespace: tailscale
spec:
  values:
    operatorConfig:
      defaultTags:
        - tag:mbk-operator
      hostname: mbk-operator
    proxyConfig:
      defaultTags: tag:mbk
