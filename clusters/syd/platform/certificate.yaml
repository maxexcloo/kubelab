apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard
  namespace: networking
spec:
  secretName: wildcard
  dnsNames:
    - "*.syd.excloo.dev"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns01
