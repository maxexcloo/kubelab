apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
  namespace: tailscale
spec:
  values:
    operatorConfig:
      hostname: syd-operator
      defaultTags:
        - tag:syd-operator
    proxyConfig:
      defaultTags: tag:syd
