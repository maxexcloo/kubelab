apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bifrost
  namespace: bifrost
spec:
  data:
    - remoteRef:
        key: bifrost
        property: api-key
      secretKey: BIFROST_API_KEY
    - remoteRef:
        key: bifrost
        property: cliproxy-api-key
      secretKey: CLIPROXY_API_KEY
    - remoteRef:
        key: bifrost
        property: comfy-control-api-key
      secretKey: COMFY_CONTROL_API_KEY
    - remoteRef:
        key: bifrost
        property: encryption-key
      secretKey: encryption-key
    - remoteRef:
        key: bifrost
        property: password
      secretKey: password
    - remoteRef:
        key: bifrost
        property: username
      secretKey: username
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    creationPolicy: Owner
    name: bifrost
