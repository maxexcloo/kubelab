apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: linkwarden
  namespace: linkwarden
spec:
  interval: 30m
  releaseName: linkwarden
  chart:
    spec:
      chart: app-template
      interval: 24h
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      version: 5.2.1
  values:
    controllers:
      linkwarden:
        strategy: Recreate
        containers:
          linkwarden:
            image:
              pullPolicy: IfNotPresent
              repository: ghcr.io/linkwarden/linkwarden
              tag: v2.16.3@sha256:0664c28a039bda8ffa6663389e8536b2fc717219e329f85b83faca7efed3b144
            env:
              BASE_URL: https://links.excloo.com
              EMAIL_FROM: linkwarden@excloo.dev
              KEYCLOAK_CUSTOM_NAME: Excloo ID
              KEYCLOAK_ISSUER: https://id.excloo.com
              MEILI_HOST: http://linkwarden-meilisearch:7700
              NEXTAUTH_INTERNAL_URL: http://localhost:3000/api/v1/auth
              NEXTAUTH_URL: https://links.excloo.com/api/v1/auth
              NEXT_PUBLIC_EMAIL_PROVIDER: "true"
              NEXT_PUBLIC_KEYCLOAK_ENABLED: "true"
              OPENROUTER_MODEL: openrouter/auto
              PLAYWRIGHT_BROWSERS_PATH: /ms-playwright
              PORT: "3000"
              TZ: Australia/Sydney
            envFrom:
              - secretRef:
                  name: linkwarden
            probes:
              readiness:
                custom: true
                enabled: true
                spec:
                  periodSeconds: 10
                  timeoutSeconds: 5
                  tcpSocket:
                    port: 3000
              startup:
                custom: true
                enabled: true
                spec:
                  failureThreshold: 60
                  periodSeconds: 10
                  timeoutSeconds: 5
                  tcpSocket:
                    port: 3000
            resources:
              limits:
                memory: 2Gi
              requests:
                cpu: 50m
                memory: 512Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
      meilisearch:
        strategy: Recreate
        containers:
          meilisearch:
            image:
              pullPolicy: IfNotPresent
              repository: docker.io/getmeili/meilisearch
              tag: v1.54.0@sha256:0bf32debcbfa8ba4e418679025f4935884972171513c92f0584689ff994a61df
            env:
              MEILI_HTTP_ADDR: 0.0.0.0:7700
              MEILI_MASTER_KEY:
                valueFrom:
                  secretKeyRef:
                    key: MEILI_MASTER_KEY
                    name: linkwarden
              MEILI_NO_ANALYTICS: "true"
              MEILI_SERVER_PROVIDER: docker
              MEILI_UPGRADE_DB: "true"
              TZ: Australia/Sydney
            probes:
              readiness:
                custom: true
                enabled: true
                spec:
                  periodSeconds: 10
                  timeoutSeconds: 5
                  httpGet:
                    path: /health
                    port: 7700
              startup:
                custom: true
                enabled: true
                spec:
                  failureThreshold: 60
                  periodSeconds: 10
                  timeoutSeconds: 5
                  httpGet:
                    path: /health
                    port: 7700
            resources:
              limits:
                memory: 512Mi
              requests:
                cpu: 20m
                memory: 128Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
    defaultPodOptions:
      automountServiceAccountToken: false
      securityContext:
        fsGroup: 568
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 568
        runAsNonRoot: true
        runAsUser: 568
        seccompProfile:
          type: RuntimeDefault
    global:
      nameOverride: linkwarden
    persistence:
      data:
        existingClaim: data
        type: persistentVolumeClaim
        advancedMounts:
          linkwarden:
            linkwarden:
              - path: /data/data
      meilisearch:
        accessMode: ReadWriteOnce
        size: 1Gi
        storageClass: local-path
        type: persistentVolumeClaim
        advancedMounts:
          meilisearch:
            meilisearch:
              - path: /meili_data
      temporary:
        type: emptyDir
        globalMounts:
          - path: /tmp
    service:
      linkwarden:
        controller: linkwarden
        ports:
          http:
            port: 3000
            protocol: HTTP
      meilisearch:
        controller: meilisearch
        ports:
          http:
            port: 7700
            protocol: HTTP
    route:
      private:
        hostnames:
          - linkwarden.mbk.excloo.dev
        labels:
          app.kubernetes.io/name: linkwarden
        parentRefs:
          - name: private
            namespace: networking
        rules:
          - backendRefs:
              - name: linkwarden
                port: 3000
      public:
        annotations:
          external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
          gethomepage.dev/description: Bookmark Manager
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Applications
          gethomepage.dev/href: https://links.excloo.com
          gethomepage.dev/icon: linkwarden
          gethomepage.dev/name: Linkwarden
          gethomepage.dev/weight: "-100"
          gethomepage.dev/widget.hideErrors: "true"
          gethomepage.dev/widget.key: '{{ "{{HOMEPAGE_VAR_LINKWARDEN_KEY}}" }}'
          gethomepage.dev/widget.type: linkwarden
          gethomepage.dev/widget.url: https://links.excloo.com
        hostnames:
          - links.excloo.com
        labels:
          app.kubernetes.io/name: linkwarden
          gateway.excloo.dev/public-access: "true"
        parentRefs:
          - name: public-tunnel
            namespace: networking
        rules:
          - backendRefs:
              - name: linkwarden
                port: 3000
