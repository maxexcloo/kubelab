apiVersion: v1
kind: PersistentVolume
metadata:
  name: shelfmark-bookdrop
  labels:
    app.kubernetes.io/name: shelfmark
spec:
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 1Gi
  mountOptions:
    - nfsvers=4.2
  nfs:
    path: /mnt/truenas-nvme/bookorbit/bookdrop
    server: 10.4.0.3
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
