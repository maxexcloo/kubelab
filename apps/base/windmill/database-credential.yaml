apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: windmill-database
  namespace: windmill
spec:
  allowRepeat: true
  digits: 12
  length: 48
  noUpper: false
  symbols: 12
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: windmill-database
  namespace: windmill
spec:
  deletionPolicy: None
  refreshInterval: 1h
  updatePolicy: IfNotExists
  data:
    - match:
        remoteRef:
          remoteKey: Windmill
      metadata:
        apiVersion: kubernetes.external-secrets.io/v1alpha1
        kind: PushSecretMetadata
        spec:
          tags: [Homelab, Database, Windmill]
  secretStoreRefs:
    - kind: ClusterSecretStore
      name: onepassword
  selector:
    generatorRef:
      apiVersion: generators.external-secrets.io/v1alpha1
      kind: Password
      name: windmill-database
  template:
    engineVersion: v2
    data:
      password: "{{ .password }}"
      username: windmill
