apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bichon
  namespace: bichon
  annotations:
    onepassword.excloo.dev/generate-fields: encryption-password
spec:
  refreshInterval: 1h
  data:
    - remoteRef:
        key: Bichon
        property: encryption-password
      secretKey: BICHON_ENCRYPT_PASSWORD
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    creationPolicy: Owner
    name: bichon
