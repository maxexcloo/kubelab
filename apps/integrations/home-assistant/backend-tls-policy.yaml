apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: home-assistant
  namespace: home-assistant-webhook
spec:
  targetRefs:
    - group: ""
      kind: Service
      name: home-assistant
      sectionName: https
  validation:
    hostname: hass.mbk.excloo.net
    wellKnownCACertificates: System
