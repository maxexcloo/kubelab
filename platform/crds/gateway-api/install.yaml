apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gateway-api-crds
  namespace: flux-system
spec:
  interval: 1h
  path: ./config/crd
  prune: true
  sourceRef:
    kind: GitRepository
    name: gateway-api
  timeout: 5m
  wait: true
