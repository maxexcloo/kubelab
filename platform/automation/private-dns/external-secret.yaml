apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: control-d-credentials
  namespace: crossplane-system
spec:
  refreshInterval: 1h
  data:
    - remoteRef:
        key: Control D
        property: password
      secretKey: password
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    creationPolicy: Owner
    deletionPolicy: Retain
    name: control-d-credentials
    template:
      engineVersion: v2
      data:
        authorization: "Bearer {{ .password }}"
